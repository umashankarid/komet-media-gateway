{
  "name": "komet-media-gateway",
  "version": "0.2.0",
  "description": "Controllable per-court media gateway: SRT ingest -> FFmpeg -> YouTube RTMP, driven by Komet Control.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "4.21.0"
  }
}
